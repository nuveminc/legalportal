
LegalPortal.controller('config.default.view.controller',
	['$scope', 'PORTLET',
    function ($scope, PORTLET) {
        'use strict';

        var init = function () {
          if(PORTLET.setup){

          } else {

          }
        };

        init();
    }
]);
